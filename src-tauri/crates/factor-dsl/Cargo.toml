[package]
name = "desic-factor-dsl"
version = "0.1.0"
description = "Deterministic cross-sectional factor expressions over confirmed K-line panels"
license = "MIT"
edition = "2021"
rust-version = "1.77"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
desic-chart-dsl = { path = "../chart-dsl" }

[dev-dependencies]
serde_json = "1"
