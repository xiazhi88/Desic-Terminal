[package]
name = "desic-systematic"
version = "0.1.0"
description = "Deterministic domain primitives for systematic research and paper backtests"
license = "MIT"
edition = "2021"
rust-version = "1.77"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
desic-factor-dsl = { path = "../factor-dsl" }
